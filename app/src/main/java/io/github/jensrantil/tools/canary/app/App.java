/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package io.github.jensrantil.tools.canary.app;

import io.github.jensrantil.tools.canary.list.LinkedList;

import static io.github.jensrantil.tools.canary.utilities.StringUtils.join;
import static io.github.jensrantil.tools.canary.utilities.StringUtils.split;
import static io.github.jensrantil.tools.canary.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
